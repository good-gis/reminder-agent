# 🔔 Reminder Agent - Планировщик задач с MCP

Агент для управления задачами, работающий 24/7 и предоставляющий сводки через OpenAI-совместимый API.

Поддерживает:
- **OpenAI API** (GPT-4, GPT-4o-mini и др.)
- **Локальные LLM** через LM Studio, Ollama, LocalAI и другие OpenAI-совместимые серверы

## Архитектура

```
┌─────────────────┐     ┌─────────────────┐     ┌──────────────┐
│  LLM API        │◄────│   MCP Server    │◄────│  tasks.json  │
│  (OpenAI/Local) │     │   (reminder)    │     │              │
└────────┬────────┘     └─────────────────┘     └──────────────┘
         │
         ▼
┌─────────────────┐
│  Уведомления    │
│  (консоль)      │
└─────────────────┘
```

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка LLM

**Вариант A: OpenAI API**
```bash
export OPENAI_API_KEY=your-api-key-here
```

**Вариант B: Локальная LLM (LM Studio, Ollama и др.)**
```bash
export LLM_BASE_URL=http://127.0.0.1:1234/v1  # URL вашего локального сервера
export LLM_MODEL=your-model-name               # опционально
```

### 3. Сборка

```bash
npm run build
```

### 4. Запуск

**Режим демона (24/7 с периодическими уведомлениями):**
```bash
npm run start:agent
# или
node dist/agent.js
```

**Интерактивный режим:**
```bash
node dist/agent.js interactive
# или
node dist/agent.js -i
```

**Однократная сводка:**
```bash
node dist/agent.js once
```

## Режимы работы

### 1. Daemon Mode (по умолчанию)
Агент работает постоянно и выдаёт сводку по расписанию (по умолчанию каждые 30 минут).

```bash
node dist/agent.js
```

Изменить расписание:
```bash
REMINDER_CRON="0 9 * * *" node dist/agent.js  # Каждый день в 9:00
REMINDER_CRON="*/5 * * * *" node dist/agent.js  # Каждые 5 минут
```

### 2. Interactive Mode
Режим диалога с агентом.

```bash
node dist/agent.js -i
```

Доступные команды:
- `summary` - получить сводку
- `exit` - выход
- Любой текст - отправить запрос OpenAI

Примеры запросов:
- "Добавь задачу: купить молоко, приоритет high, до завтра"
- "Какие у меня просроченные задачи?"
- "Отметь задачу 1 как выполненную"
- "Покажи задачи с тегом работа"

### 3. Once Mode
Получить сводку один раз и завершить работу.

```bash
node dist/agent.js once
```

## MCP инструменты

MCP сервер предоставляет следующие инструменты:

| Инструмент | Описание |
|------------|----------|
| `get_tasks` | Получить список задач с фильтрами |
| `get_task_summary` | Полная сводка по задачам |
| `get_task_by_id` | Детали конкретной задачи |
| `add_task` | Добавить новую задачу |
| `update_task_status` | Обновить статус задачи |
| `delete_task` | Удалить задачу |
| `get_overdue_tasks` | Просроченные задачи |
| `get_today_tasks` | Задачи на сегодня |

## Структура задачи

```typescript
interface Task {
  id: string;
  title: string;
  description?: string;
  priority: 'low' | 'medium' | 'high' | 'critical';
  status: 'pending' | 'in_progress' | 'completed' | 'overdue';
  dueDate?: string;  // ISO date
  tags?: string[];
  createdAt: string;
  updatedAt: string;
}
```

## Файл задач

Задачи хранятся в `data/tasks.json`. Вы можете редактировать его вручную или через агента.

Пример:
```json
{
  "tasks": [
    {
      "id": "1",
      "title": "Подготовить отчёт",
      "priority": "high",
      "status": "in_progress",
      "dueDate": "2025-12-18T10:00:00.000Z",
      "tags": ["работа"]
    }
  ],
  "lastUpdated": "2025-12-17T08:00:00.000Z"
}
```

## Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `OPENAI_API_KEY` | API ключ OpenAI | *обязателен для OpenAI* |
| `LLM_BASE_URL` | URL OpenAI-совместимого API | `https://api.openai.com/v1` |
| `LLM_MODEL` | Название модели | `gpt-4o-mini` |
| `LLM_API_KEY` | API ключ (альтернатива OPENAI_API_KEY) | — |
| `REMINDER_CRON` | Расписание уведомлений (cron) | `*/30 * * * *` |
| `TASKS_FILE` | Путь к файлу задач | `data/tasks.json` |

### Примеры конфигурации

**LM Studio:**
```bash
LLM_BASE_URL=http://127.0.0.1:1234/v1 node dist/agent.js interactive
```

**Ollama:**
```bash
LLM_BASE_URL=http://127.0.0.1:11434/v1 LLM_MODEL=llama3 node dist/agent.js interactive
```

## Использование как MCP сервер

Вы можете использовать MCP сервер отдельно с другими MCP клиентами.

### Конфигурация для MCP клиентов (например, Claude Desktop)

Добавьте в конфигурацию MCP клиента:

```json
{
  "mcpServers": {
    "reminder": {
      "command": "node",
      "args": ["/path/to/reminder-agent/dist/mcp-server.js"],
      "env": {
        "TASKS_FILE": "/path/to/tasks.json"
      }
    }
  }
}
```

## Разработка

```bash
# Запуск в dev режиме (без сборки)
npm run dev:agent

# Только MCP сервер
npm run dev:server
```

## Примеры взаимодействия

### Сводка
```
> summary

📋 Сводка по вашим задачам:

🔴 КРИТИЧНО: 1 задача требует немедленного внимания
   - Code review (до 12:00)

⚠️ ПРОСРОЧЕНО: 1 задача
   - Оплатить счета (просрочена на 1 день)

📅 СЕГОДНЯ: 2 задачи
   - Созвон с командой (14:00)
   - Code review (12:00)

Рекомендация: Сначала займитесь code review, затем 
оплатите счета, чтобы закрыть просроченную задачу.
```

### Добавление задачи
```
> Добавь задачу: позвонить маме, приоритет high, до завтра

Задача создана:
- Название: Позвонить маме
- Приоритет: high
- Срок: завтра
```

## Лицензия

MIT
