# 🔔 Reminder Agent

**Умный планировщик задач с MCP и поддержкой локальных LLM**

Агент для управления задачами, работающий 24/7 и предоставляющий сводки через OpenAI-совместимый API.

---

## ✨ Возможности

- 🤖 **AI-ассистент** — умные напоминания и рекомендации по приоритетам
- 🏠 **Локальные LLM** — работает с LM Studio, Ollama, LocalAI
- ☁️ **OpenAI API** — поддержка GPT-4, GPT-4o-mini и других моделей
- 🔧 **MCP Protocol** — интеграция с Claude Desktop и другими MCP-клиентами
- ⏰ **Режим 24/7** — автоматические уведомления по расписанию
- 💬 **Интерактивный чат** — диалог с агентом на естественном языке

---

## 🛠️ Технологии

- **TypeScript** — типизированный код
- **OpenAI SDK** — совместимость с любым OpenAI-like API
- **MCP (Model Context Protocol)** — стандарт для AI-инструментов
- **node-cron** — планировщик задач

---

## 📂 Архитектура

```
┌─────────────────┐     ┌─────────────────┐     ┌──────────────┐
│  LLM API        │◄────│   MCP Server    │◄────│  tasks.json  │
│  (OpenAI/Local) │     │   (reminder)    │     │              │
└────────┬────────┘     └─────────────────┘     └──────────────┘
         │
         ▼
┌─────────────────┐
│  📣 Уведомления │
└─────────────────┘
```

---

## 🚀 Быстрый старт

### 📥 1. Установка

```bash
npm install
```

### ⚙️ 2. Настройка LLM

**Вариант A: OpenAI API**
```bash
export OPENAI_API_KEY=your-api-key-here
```

**Вариант B: Локальная LLM (LM Studio)**
```bash
export LLM_BASE_URL=http://127.0.0.1:1234/v1
export LLM_MODEL=your-model-name  # опционально
```

**Вариант C: Ollama**
```bash
export LLM_BASE_URL=http://127.0.0.1:11434/v1
export LLM_MODEL=llama3
```

### 🔨 3. Сборка

```bash
npm run build
```

### ▶️ 4. Запуск

```bash
# 🔄 Режим демона (24/7)
npm run start:agent

# 💬 Интерактивный режим
node dist/agent.js interactive

# 📋 Однократная сводка
node dist/agent.js once
```

---

## 🎮 Режимы работы

### 🔄 Daemon Mode (по умолчанию)

Агент работает постоянно и выдаёт сводку по расписанию.

```bash
node dist/agent.js

# Изменить расписание:
REMINDER_CRON="0 9 * * *" node dist/agent.js     # Каждый день в 9:00
REMINDER_CRON="*/5 * * * *" node dist/agent.js   # Каждые 5 минут
```

### 💬 Interactive Mode

Режим диалога с агентом.

```bash
node dist/agent.js -i
```

**Команды:**
- `summary` — получить сводку
- `exit` — выход
- Любой текст — отправить запрос LLM

**Примеры запросов:**
- *"Добавь задачу: купить молоко, приоритет high, до завтра"*
- *"Какие у меня просроченные задачи?"*
- *"Отметь задачу 1 как выполненную"*
- *"Покажи задачи с тегом работа"*

### 📋 Once Mode

Получить сводку один раз и завершить работу.

```bash
node dist/agent.js once
```

---

## 🧰 MCP инструменты

| Инструмент | Описание |
|------------|----------|
| 📋 `get_tasks` | Получить список задач с фильтрами |
| 📊 `get_task_summary` | Полная сводка по задачам |
| 🔍 `get_task_by_id` | Детали конкретной задачи |
| ➕ `add_task` | Добавить новую задачу |
| ✏️ `update_task_status` | Обновить статус задачи |
| 🗑️ `delete_task` | Удалить задачу |
| ⚠️ `get_overdue_tasks` | Просроченные задачи |
| 📅 `get_today_tasks` | Задачи на сегодня |

---

## 📝 Структура задачи

```typescript
interface Task {
  id: string;
  title: string;
  description?: string;
  priority: 'low' | 'medium' | 'high' | 'critical';
  status: 'pending' | 'in_progress' | 'completed' | 'overdue';
  dueDate?: string;  // ISO date
  tags?: string[];
  createdAt: string;
  updatedAt: string;
}
```

---

## 💾 Файл задач

Задачи хранятся в `data/tasks.json`. Можно редактировать вручную или через агента.

```json
{
  "tasks": [
    {
      "id": "1",
      "title": "Подготовить отчёт",
      "priority": "high",
      "status": "in_progress",
      "dueDate": "2025-12-18T10:00:00.000Z",
      "tags": ["работа"]
    }
  ],
  "lastUpdated": "2025-12-17T08:00:00.000Z"
}
```

---

## ⚙️ Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `OPENAI_API_KEY` | API ключ OpenAI | *обязателен для OpenAI* |
| `LLM_BASE_URL` | URL OpenAI-совместимого API | — |
| `LLM_MODEL` | Название модели | `gpt-4o-mini` |
| `LLM_API_KEY` | API ключ (альтернатива OPENAI_API_KEY) | — |
| `REMINDER_CRON` | Расписание уведомлений (cron) | `*/30 * * * *` |
| `TASKS_FILE` | Путь к файлу задач | `data/tasks.json` |

---

## 🔌 Использование как MCP сервер

Интеграция с Claude Desktop и другими MCP-клиентами:

```json
{
  "mcpServers": {
    "reminder": {
      "command": "node",
      "args": ["/path/to/reminder-agent/dist/mcp-server.js"],
      "env": {
        "TASKS_FILE": "/path/to/tasks.json"
      }
    }
  }
}
```

---

## 🧑‍💻 Разработка

```bash
# Запуск в dev режиме (без сборки)
npm run dev:agent

# Только MCP сервер
npm run dev:server
```

---

## 💡 Примеры взаимодействия

### 📊 Сводка
```
> summary

📋 Сводка по вашим задачам:

🔴 КРИТИЧНО: 1 задача требует немедленного внимания
   - Code review (до 12:00)

⚠️ ПРОСРОЧЕНО: 1 задача
   - Оплатить счета (просрочена на 1 день)

📅 СЕГОДНЯ: 2 задачи
   - Созвон с командой (14:00)
   - Code review (12:00)

💡 Рекомендация: Сначала займитесь code review, затем
оплатите счета, чтобы закрыть просроченную задачу.
```

### ➕ Добавление задачи
```
> Добавь задачу: позвонить маме, приоритет high, до завтра

✅ Задача создана:
   - Название: Позвонить маме
   - Приоритет: high
   - Срок: завтра
```

---

## 📄 Лицензия

MIT

---

✨ Made with ❤️ by [good-gis](https://github.com/good-gis/) ✨
